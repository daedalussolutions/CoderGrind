<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <div class="ml-auto order-0"><a class="navbar-brand ml-auto" href="/"><%= image_tag("Logo.png", width: '150')%></a></div>
      <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
          <li class="nav-item">
            <div class="nav-link"><%= link_to "#{@user.username}", account_path %></div>
          </li>
          <li class="nav-item">
            <div><%= image_tag("icons/fire.ico", width: '30px')%></div>
          </li>
          <div class="daily-streak-counter">
            <div class="nav-item"><%= "#{@statistics.streak}" %></div>
          </div>
          <li class="nav-item">
            <div class="nav-link"><%= button_to "Log Out", session_path, method: :delete %></div>
          </li>
        <% else %>
          <li class="nav-item">
            <div class="nav-link"><%= link_to "Sign Up", new_registration_path %></div>
          </li>
        <% end %>
      </ul>
  </div>
</nav>
<div><%= alert %></div>
  